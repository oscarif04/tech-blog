<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  {{!-- <link rel="stylesheet" href="/stylesheets/style.css"> --}}
  <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>
  <div class="border">
    <header>
        <h1  class="text-gray-600 body-font text-5xl text-center">My Tech Blog
        </h1>
        <nav class="text-white bg-blue-500 navbar mt-2">
            {{#if loggedIn}}
            <a href="/" class="navbar-item is-hoverable pt-1 text-white">Home Page</a>
            <a href="/dashboard" class="navbar-item is-hoverable pt-1 text-white">Dashboard</a>
            <a href="/logout" id="logout" class="navbar-item is-pulled-right is-hoverable pt-1 mr-4 text-white">Logout</a>
            {{!-- <button id="logout" class="button is-danger">Logout</button> --}}
            {{else}}
            <a href="/" class="navbar-item is-hoverable pt-1">Home Page</a>
            <a href="/login" class="login-form" class="button is-primary">Login</a>
            {{/if}}
        </nav>
    </header>
    <main>
            {{{ body }}}
    </main>
    <footer class="footer rows pb-2">
        <div class="row-is-full content has-text-centered">
            <p>
            <a href="/">Home Page</a>{{#if loggedIn}} | 
            <a href="/dashboard/">Dashboard</a>
            {{else}}
             | <a href="/login/">Login</a>
            {{/if}}
            </p>
        </div>
      <div class="row-is-full content has-text-centered">
  </div>
    </footer>
  </div>
{{#if loggedIn}}
<script src="/JS/logout.js"></script>
{{/if}}
</body>
</html>